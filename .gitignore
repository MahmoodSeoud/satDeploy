__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/
notes/features/
.DS_Store
.gstack/
build/
build-native/
build-arm/
dist/

# Experiment harness — CSV outputs are reproducible from the scripts in
# experiments/runs/, so we don't track them. (Per-trial log directories
# land in /tmp/satdeploy-experiments/, also excluded.)
experiments/results/

# Vendored build inputs fetched into the APM lib tree (meson packagecache,
# unpacked SQLite amalgamation). Not source — regenerated by the build, and
# 14 MB we don't want in history.
satdeploy-apm/lib/packagecache/
satdeploy-apm/lib/sqlite-amalgamation-*/
